# ============================================
# SECURITY: Secrets and sensitive files
# ============================================
.env                          # API keys, credentials, local settings
.env.local                    # Local environment overrides
*.key                         # Private keys
*.pem                         # Certificates
*.cert                        # SSL certificates

# ============================================
# SECURITY: Sensitive configuration files
# ============================================
# Only aflow_llm.yaml is ignored (contains API keys via env variables)
# training.yaml and models.yaml are tracked (contain only hyperparameters)
config/aflow_llm.yaml
!config/aflow_llm.yaml.example

# ============================================
# DATA: Training and evaluation datasets
# ============================================
# Raw data files - too large to track in git
*.jsonl                       # JSONL data files
!data/sample*.jsonl           # Allow sample data for testing
data/raw/                     # Downloaded raw datasets
data/processed/               # Processed dataset splits
data/mixed/                   # Mixed training datasets
!data/.gitkeep                # Keep directory structure

# ============================================
# MODELS: Pretrained and fine-tuned models
# ============================================
models/                       # Downloaded pretrained models
!models/.gitkeep              # Keep directory structure

# ============================================
# CHECKPOINTS: Training outputs (run-time generated)
# ============================================
checkpoints/                  # LoRA checkpoints and model weights
!checkpoints/.gitkeep         # Keep directory structure

# ============================================
# LOGS: Training and evaluation logs
# ============================================
logs/                         # Training logs
!logs/.gitkeep                # Keep directory structure
wandb/                        # Weights & Biases experiment logs
nohup.out                     # Background process output
*.log                         # General log files

# ============================================
# PYTHON: Virtual environments and build artifacts
# ============================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ============================================
# JUPYTER: Notebook temporary files
# ============================================
.ipynb_checkpoints/           # Jupyter cell cache

# ============================================
# VIRTUALENV: Python virtual environments
# ============================================
venv/
ENV/
env/
.venv

# ============================================
# IDEs: Editor and IDE configuration
# ============================================
.vscode/                      # Visual Studio Code
.idea/                        # JetBrains PyCharm
*.swp                         # Vim swap files
*.swo                         # Vim backup files
*~                            # Vim recovery files

# ============================================
# OS: Operating system files
# ============================================
.DS_Store                     # macOS
Thumbs.db                     # Windows

